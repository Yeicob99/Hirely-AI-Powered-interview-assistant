import React from 'react'

const InterviewCard = ({interviewId, userId, role, type, techstack, createdAt}: InterviewCardProps) => {
    /// Nos quedamos aqui => const feedback = nul
    return (
    <div>InterviewCard</div>
  )
}

export default InterviewCard